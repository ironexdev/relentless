#!/bin/sh

. bin/docker/env.secret

export USER_ID=$(id -u)
export GROUP_ID=$(id -u)

# App
export APP_ENV=dev
export SERVER_NAME="http://localhost:80"
export PHP_DATE_TIMEZONE="UTC"
export CORS_ALLOW_ORIGIN=['http://localhost']
export GRAPHQL_ENABLE_INTROSPECTION=0

# DB
export DATABASE_VERSION="17.5"
export DATABASE_HOST="db"
export DATABASE_PORT=5432
export DATABASE_NAME="relentlessdb"
export DATABASE_USER="relentlessuser"

echo "✓ Environment variables set."
